import { RepeatedPasswordChartProps } from "./RepeatedPasswordChart.types";

const RepeatedPasswordChart = ({ }:RepeatedPasswordChartProps) => {
  return <div>RepeatedPasswordChart</div>;
};

export default RepeatedPasswordChart;
